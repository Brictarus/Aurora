export type Color = string;
export type TileValue = number;
export type Terrain = TileValue[][];

export type Point = { x: number, y: number };
export type Rectangle = { x: number, y: number, w: number, h: number };